# PageSprite
PageSprite — AI-powered static page generator with canvas annotation workflow. Draw rectangles on an infinite canvas,    prompt AI per-region to generate HTML content, and iterate with visual annotations. Built with Tauri 2 + React 19 +   Rust.
